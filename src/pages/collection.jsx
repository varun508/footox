import React from 'react';
import { CollectionContainer } from '../containers';

const collection = () => {
    return (
        <div>
            <CollectionContainer />
        </div>
    );
};

export default collection;
